<Window x:Class="Siv.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Siv.Desktop.Views"
        xmlns:viewModels="clr-namespace:Siv.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="SIV - Sistema de Información de Vuelos" Height="768" Width="1024"
        Background="{StaticResource BrushFondoOscuro}"
        Foreground="{StaticResource BrushTextoClaro}"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <DataTemplate DataType="{x:Type viewModels:VuelosViewModel}">
            <local:VuelosView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:AerolineasViewModel}">
            <local:AerolineasView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:AeropuertosViewModel}">
            <local:AeropuertosView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:CambiosOperativosViewModel}">
            <local:CambiosOperativosView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:SeguimientosViewModel}">
            <local:SeguimientosView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:NotificacionesViewModel}">
            <local:NotificacionesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:AuditoriasViewModel}">
            <local:AuditoriasView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:HistorialEstadosViewModel}">
            <local:HistorialEstadosView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Menú Lateral -->
        <Border Grid.Column="0" Background="{StaticResource BrushSuperficie}">
            <StackPanel>
                <TextBlock Text="SIV Desktop" FontSize="20" FontWeight="Bold" Margin="20,30,20,40" Foreground="{StaticResource BrushTextoClaro}" />
                
                <Button Content="✈️ Vuelos" Command="{Binding NavigateToVuelosCommand}" Style="{StaticResource BotonMenuNavegacion}" Visibility="{Binding PuedeConsultarVuelos, Converter={StaticResource BoolToVisibilityConverter}}" />
                <Button Content="🏢 Aerolíneas" Command="{Binding NavigateToAerolineasCommand}" Style="{StaticResource BotonMenuNavegacion}" Visibility="{Binding EsAdmin, Converter={StaticResource BoolToVisibilityConverter}}" />
                <Button Content="📍 Aeropuertos" Command="{Binding NavigateToAeropuertosCommand}" Style="{StaticResource BotonMenuNavegacion}" Visibility="{Binding EsAdmin, Converter={StaticResource BoolToVisibilityConverter}}" />
                <Button Content="🔄 Cambios operativos" Command="{Binding NavigateToCambiosOperativosCommand}" Style="{StaticResource BotonMenuNavegacion}" Visibility="{Binding PuedeGestionarCambios, Converter={StaticResource BoolToVisibilityConverter}}" />
                <Button Content="📜 Historial de estados" Command="{Binding NavigateToHistorialEstadosCommand}" Style="{StaticResource BotonMenuNavegacion}" Visibility="{Binding PuedeConsultarHistorial, Converter={StaticResource BoolToVisibilityConverter}}" />
                <Button Content="🔔 Notificaciones" Command="{Binding NavigateToNotificacionesCommand}" Style="{StaticResource BotonMenuNavegacion}" Visibility="{Binding PuedeConsultarNotificaciones, Converter={StaticResource BoolToVisibilityConverter}}" />
                <Button Content="📜 Auditoría" Command="{Binding NavigateToAuditoriasCommand}" Style="{StaticResource BotonMenuNavegacion}" Visibility="{Binding PuedeConsultarAuditoria, Converter={StaticResource BoolToVisibilityConverter}}" />

                <Separator Margin="10,20,10,20" Background="#444" />
                <Button Content="🚪 Cerrar Sesión" Command="{Binding CerrarSesionCommand}" Style="{StaticResource BotonMenuNavegacion}" Foreground="#FF6B6B" />
            </StackPanel>
        </Border>

        <!-- Contenido Principal -->
        <Border Grid.Column="1" Padding="30">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Border>
    </Grid>
</Window>
