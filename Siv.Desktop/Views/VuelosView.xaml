<UserControl x:Class="Siv.Desktop.Views.VuelosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gestión de Vuelos" Style="{StaticResource TituloPagina}" />

        <Border Grid.Row="1" Padding="12" Margin="0,0,0,12" Background="{StaticResource BrushSuperficie}"
                Visibility="{Binding EsAdmin, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="95"/><ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="95"/><ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Número" VerticalAlignment="Center" Margin="0,4" />
                <TextBox Grid.Column="1" Text="{Binding NumeroVueloNuevo, UpdateSourceTrigger=PropertyChanged}" Margin="5" />
                <TextBlock Grid.Column="2" Text="Puerta" VerticalAlignment="Center" Margin="8,4,0,4" />
                <TextBox Grid.Column="3" Text="{Binding PuertaNueva, UpdateSourceTrigger=PropertyChanged}" Margin="5" />
                <TextBlock Grid.Row="1" Text="Aerolínea" VerticalAlignment="Center" Margin="0,4" />
                <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Aerolineas}" DisplayMemberPath="Nombre" SelectedItem="{Binding AerolineaNueva}" Margin="5" />
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Origen" VerticalAlignment="Center" Margin="8,4,0,4" />
                <ComboBox Grid.Row="1" Grid.Column="3" ItemsSource="{Binding Aeropuertos}" DisplayMemberPath="Nombre" SelectedItem="{Binding AeropuertoOrigenNuevo}" Margin="5" />
                <TextBlock Grid.Row="2" Text="Destino" VerticalAlignment="Center" Margin="0,4" />
                <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Aeropuertos}" DisplayMemberPath="Nombre" SelectedItem="{Binding AeropuertoDestinoNuevo}" Margin="5" />
                <TextBlock Grid.Row="2" Grid.Column="2" Text="Horario" VerticalAlignment="Center" Margin="8,4,0,4" />
                <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding HorarioProgramadoNuevo, UpdateSourceTrigger=PropertyChanged}" ToolTip="dd/MM/yyyy HH:mm" Margin="5" />
                <TextBlock Grid.Row="3" Text="Visibilidad" VerticalAlignment="Center" Margin="0,4" />
                <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding NivelesVisibilidad}" SelectedItem="{Binding NivelVisibilidadNuevo}" Margin="5" />
                <Button Grid.Row="3" Grid.Column="3" Content="Crear vuelo" Command="{Binding CrearVueloCommand}" Style="{StaticResource BotonPrimario}" Margin="5" HorizontalAlignment="Right" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Refrescar" Command="{Binding CargarDatosCommand}" Style="{StaticResource BotonPrimario}" />
        </StackPanel>

        <DataGrid Grid.Row="3" ItemsSource="{Binding Vuelos}" SelectedItem="{Binding VueloSeleccionado}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Vuelo ID" Binding="{Binding VueloId}" Width="80" />
                <DataGridTextColumn Header="Número" Binding="{Binding NumeroVuelo}" />
                <DataGridTextColumn Header="Aerolínea" Binding="{Binding AerolineaNombre}" />
                <DataGridTextColumn Header="Origen" Binding="{Binding AeropuertoOrigenNombre}" />
                <DataGridTextColumn Header="Destino" Binding="{Binding AeropuertoDestinoNombre}" />
                <DataGridTextColumn Header="Horario" Binding="{Binding HorarioProgramado, StringFormat=dd/MM/yyyy HH:mm}" />
                <DataGridTextColumn Header="Puerta" Binding="{Binding Puerta}" />
                <DataGridTemplateColumn Header="Estado">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding EstadoVueloId, Converter={StaticResource EstadoVueloToColorConverter}}" 
                                    CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding EstadoVueloNombre}" Foreground="White" FontWeight="Bold" FontSize="11" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
