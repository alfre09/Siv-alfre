<UserControl x:Class="Siv.Desktop.Views.CambiosOperativosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Cambios Operativos" Style="{StaticResource TituloPagina}" />

        <Border Grid.Row="1" Padding="15" Margin="0,0,0,15"
                Background="{StaticResource BrushSuperficie}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Text="Vuelo" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox Grid.Column="1" ItemsSource="{Binding Vuelos}" DisplayMemberPath="NumeroVuelo" SelectedValuePath="VueloId" SelectedItem="{Binding VueloSeleccionado}" Margin="5"/>

                <TextBlock Grid.Column="2" Text="Tipo" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox Grid.Column="3" ItemsSource="{Binding TiposCambio}"
                          SelectedItem="{Binding TipoCambioSeleccionado}" Margin="5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Nuevo horario" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NuevoHorarioTexto, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Ejemplo: 02/08/2026 18:30" Margin="5"/>

                <TextBlock Grid.Row="1" Grid.Column="2" Text="Nueva puerta" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox Grid.Row="1" Grid.Column="3" ItemsSource="{Binding Puertas}"
                          DisplayMemberPath="Codigo" SelectedItem="{Binding PuertaSeleccionada}" Margin="5"/>

                <TextBlock Grid.Row="1" Grid.Column="4" Text="Nuevo estado" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox Grid.Row="1" Grid.Column="5" ItemsSource="{Binding Estados}"
                          DisplayMemberPath="Nombre" SelectedItem="{Binding EstadoSeleccionado}" Margin="5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Causa" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="4"
                         Text="{Binding Causa, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <Button Grid.Row="2" Grid.Column="5" Content="Registrar cambio"
                        Command="{Binding RegistrarCambioCommand}"
                        Style="{StaticResource BotonPrimario}" Margin="5"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Cambios}"
                  SelectedItem="{Binding CambioSeleccionado}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CambioOperativoId}" Width="Auto"/>
                <DataGridTextColumn Header="Vuelo" Binding="{Binding VueloId}" Width="Auto"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoCambio}" Width="Auto"/>
                <DataGridTextColumn Header="Causa" Binding="{Binding Causa}" Width="*"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaCambio, StringFormat=dd/MM/yyyy HH:mm}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
