<UserControl x:Class="Siv.Desktop.Views.AeropuertosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Aeropuertos" Style="{StaticResource TituloPagina}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Refrescar" Command="{Binding CargarDatosCommand}" Style="{StaticResource BotonPrimario}" />
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Aeropuertos}" SelectedItem="{Binding AeropuertoSeleccionado}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="Auto" />
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />
                <DataGridTextColumn Header="Ciudad" Binding="{Binding Ciudad}" Width="*" />
                <DataGridTextColumn Header="País" Binding="{Binding Pais}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
