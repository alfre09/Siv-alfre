<UserControl x:Class="Siv.Desktop.Views.NotificacionesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Notificaciones" Style="{StaticResource TituloPagina}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Refrescar" Command="{Binding CargarDatosCommand}" Style="{StaticResource BotonPrimario}" Margin="0,0,10,0" />
            <Button Content="Marcar como Leída" Command="{Binding MarcarComoLeidaCommand}" CommandParameter="{Binding NotificacionSeleccionada}" Style="{StaticResource BotonPrimario}" />
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Notificaciones}" SelectedItem="{Binding NotificacionSeleccionada}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="Auto" />
                <DataGridTextColumn Header="Vuelo" Binding="{Binding VueloId}" Width="Auto" />
                <DataGridTextColumn Header="Cambio" Binding="{Binding CambioOperativoId}" Width="Auto" />
                <DataGridTextColumn Header="Mensaje" Binding="{Binding Mensaje}" Width="*" />
                <DataGridTextColumn Header="Leída" Binding="{Binding Leida, Converter={StaticResource BooleanToLeidaTextConverter}}" Width="Auto" />
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaEnvio, StringFormat=dd/MM/yyyy HH:mm}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
