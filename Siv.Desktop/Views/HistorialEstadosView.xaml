<UserControl x:Class="Siv.Desktop.Views.HistorialEstadosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="Historial de estados del vuelo" Style="{StaticResource TituloPagina}" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox Width="160" Text="{Binding VueloIdTexto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" ToolTip="ID de vuelo" />
            <Button Content="Consultar" Command="{Binding ConsultarCommand}" Style="{StaticResource BotonPrimario}" />
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Historial}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Vuelo" Binding="{Binding VueloId}" Width="80" />
                <DataGridTextColumn Header="Estado anterior" Binding="{Binding EstadoAnteriorNombre}" Width="*" />
                <DataGridTextColumn Header="Estado nuevo" Binding="{Binding EstadoNuevoNombre}" Width="*" />
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaCambio, StringFormat=dd/MM/yyyy HH:mm}" Width="160" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
