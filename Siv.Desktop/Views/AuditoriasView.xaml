<UserControl x:Class="Siv.Desktop.Views.AuditoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Registro de Auditoría" Style="{StaticResource TituloPagina}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Refrescar" Command="{Binding CargarDatosCommand}" Style="{StaticResource BotonPrimario}" />
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Auditorias}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy HH:mm:ss}" Width="Auto" />
                <DataGridTextColumn Header="Acción" Binding="{Binding Accion}" Width="Auto" />
                <DataGridTextColumn Header="Tabla" Binding="{Binding Tabla}" Width="Auto" />
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
